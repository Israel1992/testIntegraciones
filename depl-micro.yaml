apiVersion: batch/v1
kind: Job
metadata:
  name: micro-job-test-integraciones
spec:
  template:
    metadata:
      name: micro-job-test-integraciones
      labels:  
        app: micro-job-test-integraciones
    spec:
      containers:
      - name: micro-container
        image: israelglez1992/my-spring-app
        ports:
        - containerPort: 8080
        env:
          - name: USERNAME_SMTP
            value: A
          - name: PASSWORD_SMTP
            value: B
      restartPolicy: Never
  backoffLimit: 2

---
apiVersion: v1
kind: Service
metadata:
  name: micro-srv-test-integraciones
spec:
  selector:
    app: micro-job-test-integraciones
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
  type: NodePort
