apiVersion: batch/v1
kind: Job
metadata:
  name: micro-job
spec:
  template:
    metadata:
      name: micro-job
      labels:  
        app: micro-job
    spec:
      containers:
      - name: micro-container
        image: israelglez1992/my-spring-app
        ports:
        - containerPort: 8080
        env:
          - name: USERNAME_SMTP
            value: A
          - name: PASSWORD_SMTP
            value: B
      restartPolicy: Never
  backoffLimit: 2

---
apiVersion: v1
kind: Service
metadata:
  name: micro-srv
spec:
  selector:
    app: micro-job
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
  type: NodePort
